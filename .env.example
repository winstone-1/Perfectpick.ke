# Root .env.example — reference only. Actual env files live in:
#   apps/frontend/.env  (VITE_*)
#   apps/backend/.env   (PORT, MONGODB_URI, PAYSTACK_*, etc.)
# Copy per-app examples:
#   cp apps/frontend/.env.example apps/frontend/.env
#   cp apps/backend/.env.example apps/backend/.env

# See apps/backend/.env.example and apps/frontend/.env.example for full vars
